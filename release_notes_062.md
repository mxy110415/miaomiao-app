## 0.6.2
- **修**：打一句没有标点的话时，一个「喵」都不会加（末尾兜底条件的判断位置写错，那段代码从未生效）。
- **修**：改写出来的「喵」有时一闪就没 —— 改写器把自己写回的内容当成用户输入重新反推，把刚加的「喵」删掉了。
- **新增**：改写规则单元测试 10 条（纯 JVM 跑，不需要手机）。

## 0.6.1
- **新增**：「波浪线喵」可选两种方式 —— ① 句号前面加上「喵~」 ② 把句号替换成「喵~」。
- **新增**：设置页「改写细节」可自定义「插入符号」（末尾追加在哪些符号前插入）与「喵内容」（断句喵喵拳加什么字）。
- **改**：「末尾追加」改为在每个标点前插入自定义内容（默认「喵」）；「断句喵喵拳」保持原本方式。
- **新增**：可自定义「触发标点」。

---

**下载**（推荐，走本站域名，国内更稳）：
https://miaomiao.xingran666.xyz/download/miaomiao-app-latest.apk

备用（GitHub）：见下方 Assets 里的 `miaomiao-app-latest.apk`

需要 Android 7.0 及以上。
