# 更新日志

本文件按版本倒序记录改动。
**发布约定**：每次出新的安装包，都要在这里补一条，并在 GitHub 上建对应版本的 Release（附 APK）。

版本号规则：`主.中.末` —— 主位=重构，中位=加功能，末位=优化/修 bug；前一位进位时后面归 0。
未完全稳定的版本会带 `-Beta` 后缀。

---

## 0.6.3
- **改**：应用里点「去下载」不再直接跳到 GitHub 的安装包直链，改为打开**喵喵助手主页的下载页**（页面上有当前版本、下载按钮和备用通道）—— 直连 GitHub 在国内常常打不开。

## 0.6.2
- **修**：打一句**没有标点**的话时一个「喵」都不会加 —— 末尾兜底条件的判断位置写错，那段代码永远不会执行。
- **修**：改写出来的「喵」有时**一闪就没了** —— 改写器把结果写回输入框后，下一轮又把自己写的文本当成"用户输入"重新反推，把刚加的「喵」当痕迹删掉。现在读到"这就是我们自己刚写的"会直接跳过。
- **新增**：**改写规则单元测试 10 条**（纯 JVM 跑，不需要手机）。以后改规则先跑测试，不再靠装机试。

## 0.6.1
- **新增**：「波浪线喵」可选两种方式 —— ① 句号前面加上「喵~」（`你好。` → `你好喵~。`）② 把句号替换成「喵~」（`你好。` → `你好喵~`）。
- **新增**：设置页「改写细节」多出两个自定义项 —— **插入符号**（末尾追加在哪些符号前插入）、**喵内容**（断句喵喵拳加什么字，留空用内置喵库）。
- **改**：「末尾追加」改为"在**每个标点前**插入自定义内容"（默认「喵」，可自定义）；「断句喵喵拳」保持原本方式（有句末标点加在标点前，没有标点加末尾）。
- **新增**：主界面「触发模式」里可以自定义**触发标点**（默认 `。！？!?` 与空格）。

## 0.6.0
- **新增**：设置页最下面多了一块「更新」——显示当前版本、可以手动点「检测更新」、可以开关「启动时自动检测更新」。
- **新增**：打开应用自动检查一次更新，发现新版本会弹出提示（带更新说明），可以直接跳去下载。
- **说明**：检测更新读的是本仓库的 Release 信息（GitHub API，不通时回退读本仓库的 `version.json`）。
  这是本应用**唯一的联网用途**：只读版本号，不下载、不上传任何数据；不想要可以在设置里关掉。

---

## 0.5.1
- **修**：主界面点「微信」开关时，风险确认弹窗被当成了列表里的最后一项，必须把列表滑到最底才看得见。
  现在它改成正常的居中浮层（点开关立刻弹出，背景变暗），和设置页里的那个一致。

## 0.5.0
- **新增**：设置页顶部「制作人」卡片（xingran / life，带头像）。

## 0.4.0
- **新增**：主界面打开「微信」开关时弹二次风险确认（与设置页同一套文案与逻辑）。
- **新增**：随机颜文字支持自定义 —— 可以自己加、多行粘贴、一键恢复内置。
- **优化**：收敛诊断日志噪音。

## 0.3.0
- **修**：微信改写真正生效。根因是微信输入框的 id 和它外层容器共用了同一个 id，
  之前一直定位到外壳而不是真正的输入框；现在会在同 id 的多个节点里挑出可编辑的那个。

## 0.2.8
- **修**：稳定性修复（服务重连、开关状态同步）。

## 0.2.5 / 0.2.4 / 0.2.3
- **修**：微信开关按钮点不开/状态不对的系列问题。

## 0.2.2
- **修**：无障碍服务重连后丢掉 `FLAG_INCLUDE_NOT_IMPORTANT_VIEWS`，导致读不到微信的界面节点。

## 0.2.0
- **新增**：主题支持「跟随系统 / 浅色 / 深色 / AMOLED」（AMOLED 下禁用壁纸省电）。
- **新增**：卡片毛玻璃效果、壁纸模糊与卡片模糊的无级调节滑块。
- **优化**：界面留白与动效（平滑过渡，不再瞬移）。

## 0.1.0
- **重构**：应用更名「喵喵助手」（副标题 Deekseek生成·xingran），包名改为 `com.miaomiao`。
- **移除**：卡密验证/授权挡门。
- **调整**：保活通道收敛为「无障碍 + Shizuku」，不再依赖 root/Magisk。

## 0.0.1-Beta / 0.0.0-Beta-QQonly
- 早期内部迭代版本：先把 QQ 对话改写跑通（此时还不支持微信）。
